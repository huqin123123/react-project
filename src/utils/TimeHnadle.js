export default class TimeHndle {

    /**
     * 时间戳转换为时间
     * @param {*时间戳} timeStamp 
     */
    static TimeStampToDateTime(timestamp) {
        // return new Date(parseInt(timestamp,0) * 1000).toLocaleString().replace(/:\d{1,2}$/,' ');  
    }
}